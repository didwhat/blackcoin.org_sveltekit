Because they are loaded on every page, the styles for the <Header /> are located at the bottom of `$lib/styles.css`.

The sections are named after the files in CAPS CASE.

This has a small performace gain.
